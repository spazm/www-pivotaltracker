steps = FindVersion, ChangeAllVersions, CheckChangeLog, CheckVersionsMatch, DistTest, Commit, Tag, MakeDist, UploadCPAN, DistClean, Twitter

git.tagpattern = v%v
git.sign_tag = 1

twitter.config = ~/.twitterrc
twitter.distname = WWW-PivotalTracker
twitter.message = Just shipped %d %v - soon at %u

CheckChangeLog.files = ChangeLog
