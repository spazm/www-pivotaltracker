steps = FindVersion, ChangeAllVersions, CheckChangeLog, CheckVersionsMatch, DistTest, Commit, Tag, MakeDist, UploadCPAN, DistClean

git.tagpattern = v%v
git.sign_tag = 1

CheckChangeLog.files = ChangeLog
